import 'package:flutter/material.dart';

class ScheduleNavbarView extends StatelessWidget {
  const ScheduleNavbarView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hi, This is Schedule Page'),
      ),
    );
  }
}
